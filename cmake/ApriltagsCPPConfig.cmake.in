@PACKAGE_INIT@

get_filename_component(PACKAGE_PREFIX_DIR "${CMAKE_CURRENT_LIST_DIR}/../../../" ABSOLUTE)

set(ApriltagsCPP_INCLUDE_DIRS
  "${PACKAGE_PREFIX_DIR}/include/ApriltagsCPP"
  )

set(ApriltagsCPP_LIBRARIES ${PACKAGE_PREFIX_DIR}/lib/libApriltagsCPP.a)

include("${CMAKE_CURRENT_LIST_DIR}/ApriltagsCPPTargets.cmake")
