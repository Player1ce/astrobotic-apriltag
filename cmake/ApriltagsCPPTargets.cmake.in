# Create imported target ApriltagsCPP
add_library(ApriltagsCPP INTERFACE IMPORTED)

# Set target properties
set_target_properties(ApriltagsCPP PROPERTIES
  INTERFACE_INCLUDE_DIRECTORIES "${ApriltagsCPP_INCLUDE_DIRS}"
  INTERFACE_LINK_LIBRARIES "${ApriltagsCPP_LIBRARIES}"
)
